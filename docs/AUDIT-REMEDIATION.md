# 監査40項目と改修の対応（2026-09-14）

この表の「実装済み」は作業ブランチのコードを指します。本番サービスには未反映です。全問題の本番解消を意味しません。予約ルールは同日ユーザー承認済みです。

| ID | 指摘 | 対応・残作業 |
|---|---|---|
| F01 | 予約・通知APIの認証欠落 | 実装済み：管理APIをBearer認証、変更操作をPOST、公開ドキュメント停止。本番トークン設定が必要。 |
| F02 | 未登録利用者の迂回操作 | 実装済み：テキスト・一括入力・ボタン操作で顧客マスタ確認。 |
| F03 | Calendar障害が空き扱い | 実装済み：取得失敗を伝播し受付を停止。 |
| F04 | インスタンスごとのSQLite | PostgreSQL対応・本番SQLite起動拒否を実装。共有DB新設と旧データ移行は未実施。 |
| F05 | 受付期限と取得範囲の不一致 | 実装済み：2か月先の同日へ統一、月末補正、移動時間まで取得。 |
| F06 | 最終確定時の再検証不足 | 実装済み：最終受付前に最新Calendar、時間・部屋・変更元を再検証。 |
| F07 | 一括入力の検証不足 | 実装済み：日時、店舗、60分、期限、重複、空きの検証と時刻正規化。 |
| F08 | 返信失敗で管理者通知が消失 | 実装済み：受付と通知を同じDBトランザクションへ保存、retry key固定、再送API。定期実行設定が必要。 |
| F09 | Webhook障害でも200 | 実装済み：処理失敗503、署名・サイズ検査。 |
| F10 | 再配信で重複受付・取消 | 実装済み：イベントとユーザー処理の排他、受付キー、所有者・状態付き更新。 |
| F11 | 変更元の扱いが不整合 | 実装済み：変更元を承認まで保持、承認時にDB変更元を置換扱い。Calendarの変更元取消はスタッフ作業。 |
| F12 | Calendar予定をDB予約として取消 | 実装済み：種別を分離、本人照合、スタッフ通知を永続化。Calendar自動削除はしない。 |
| F13 | 通常取消導線なし | 実装済み：一覧から取消確認、期限区分を最終確認でも再判定。 |
| F14 | 待機の承諾・辞退が未保存 | 実装済み：本人・期限・現在状態・最新空きを検証しDB保存。 |
| F15 | 待機処理の行番号・競合 | DBで状態と通知を管理、シート行は内容識別子で再照合。照合直後の手動並べ替えと書込の競合は残るため、状態列を運用中に並べ替えない運用が必要。 |
| F16 | キャンセル待ちScheduler停止 | 未変更。PAUSEDの意図確認後、認証付きPOSTに更新し再開判断。 |
| F17 | DB/Calendar二重表示・承認未同期 | 実装済み：予定ID優先で重複除外、承認・却下API。スタッフ側手順の導入が必要。 |
| F18 | 名前部分一致による別人の表示 | 実装済み：ID優先、厳格な名前境界照合、同姓同名の名前照合を拒否。既存予定のID関連付けが必要。 |
| F19 | 固定休業・予約不可を無視 | 実装済み：共通検証に統合。 |
| F20 | 閉店を越える枠 | 実装済み：終了まで営業時間内。 |
| F21 | 重複件数と同時使用数の混同 | 実装済み：時間区間のピーク同時使用数で判定。 |
| F22 | 部屋不明・代替部屋の誤提示 | 実装済み：不明は保守的に使用扱い、代替候補も再検証。 |
| F23 | ホールド判定が広すぎる | 実装済み：該当時間帯を判定し、不関係な枠へ広げない。 |
| F24 | 店舗不明時の移動制約 | 実装済み：正式店舗名も判定、不明は保守的に移動時間を確保。 |
| F25 | JST +09:19、UTC表示 | 実装済み：localize/astimezoneでJSTへ統一。 |
| F26 | 来週・月内日付の誤解析 | 実装済み：専用パーサー、相対週・月文脈・ISO入力の回帰テスト。任意の自然文を完全解釈するものではない。 |
| F27 | 戻る・店舗変更で状態消失 | 実装済み：変更元とモード保持、ISO日付再利用、店舗操作を優先処理。 |
| F28 | 同じreply tokenを複数回使用 | 実装済み：複数日を一つの返信へまとめる。 |
| F29 | 月間利用回数の誤集計 | 実装済み：終了済み確定予約を集計、仮予約除外、履歴20件制限撤廃、同日終了分を含む。取消時の消化回数を請求台帳として自動計上する機能は対象外。 |
| F30 | 定数無視・無効店舗 | 実装済み：共通ルールとAPI列挙型、旧判定関数も共通化。 |
| F31 | 固定の正常ヘルスチェック | 実装済み：DB、設定、Calendar最終取得状況で200/503。監視側設定が必要。 |
| F32 | 同期Google呼出しでイベントループ停止 | 実装済み：上限付きスレッド処理をawait。 |
| F33 | Calendarページング欠落 | 実装済み：nextPageTokenを取得し続ける。 |
| F34 | シートタブ・形式・古いキャッシュ | 実装済み：タブ指定、重複ID/欠落検査、失敗時に古いデータへ戻さない。本番タブ名設定が必要。 |
| F35 | 状態と操作ボタンの無期限利用 | 実装済み：セッション・本人別操作トークンに期限、旧ボタンは再表示案内。 |
| F36 | 2027祝日欠落・誤祝日 | 実装済み：内閣府2027一覧、2026年2月24日は祝日から除外（独自休業は維持）。2028以降の更新は必要。 |
| F37 | 必須設定不足・クライアント未終了 | 実装済み：起動時検査と終了処理、LINE応答のタイムアウト。 |
| F38 | 秘密・DBの配布混入 | 実装済み：Git/Docker/Cloud Build除外、Dockerに実行ファイルのみコピー、旧診断の個人ID・ローカル資格情報パス・顧客出力を除去。過去Git履歴の秘密調査と必要な資格情報更新は別途。 |
| F39 | 危険なデプロイスクリプト | 実装済み：自動commit/push廃止、明示project、検証済みclean tree、無トラフィック配備、依存ハッシュ固定、CI追加。実際のイメージビルド・配備は未実施。 |
| F40 | テスト・文書の陳腐化 | 実装済み：オフライン回帰テスト、SQLite/PostgreSQL実DB検証、運用・移行文書、明示起動の読取専用診断。 |

## 本番反映前に残る判断

- 永続DBの配置、費用、バックアップ、接続方法。
- 現行インスタンスの未処理受付回収と移行時間帯。ローカルSQLiteのため全件回収を保証できない。
- 顧客タブ名と既存Calendar予定の本人照合、スタッフ承認・取消反映の運用。
- 待機Scheduler停止理由、通知再送ジョブ・監視の設定。
- 変更範囲が大きいため、独立テスト環境でのLINE/Google接続検証後に段階的配備。

本番移行の順序とロールバック制約は[ROLLOUT.md](ROLLOUT.md)を参照。

## 実施済み検証

- Python 3.12.12 / SQLite：69成功、PostgreSQL専用移行テスト1件は対象外。
- ローカル専用PostgreSQL 16.15：70成功（移行の成功、二重インポート拒否、通知を送らないことを含む）。
- requirements.lockのハッシュ付きインストール、pip check、git diff --check、deploy.sh構文検査。
- 本番Google/LINEへの書込や通知送信は行っていない。Docker実行環境がないためローカルコンテナビルドは未実施。
- Cloud Buildの2つの既存自動配備トリガーはいずれも `^main$` に限定されることを読み取り確認。mainへマージすると配備が走るため、移行準備完了までマージしない。
