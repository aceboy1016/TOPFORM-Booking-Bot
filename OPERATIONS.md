# 運用ガイド

## 状態の扱い

仮予約は`provisional`。スタッフがカレンダーへ本登録し、管理者LINEの「確定する」で承認すると`confirmed`になります。管理APIで対応するイベントIDを指定する従来操作も利用できます。変更要求は元予約を参照して保存し、承認後に旧DB要求を`superseded`にします。カレンダーの書き換えはスタッフの作業です。変更要求を却下した場合、旧予約は保持されます。

DBが受付状態と通知の正本です。Calendarは確定予定の参照元、キャンセル待ちシートは待機情報の入口と状態の表示先です。シート更新失敗でもDB上の回答・通知は失われません。

## API

公開：`POST /webhook`（LINE署名必須）、`GET /api/availability/{date}?store=ebisu`、`GET /`、`GET /health`。

以下は`Authorization: Bearer <ADMIN_API_TOKEN>`が必須です。トークンはブラウザや早見表のフロントエンドに埋め込まないでください。

| エンドポイント | 用途 |
|---|---|
| GET /api/requests | スタッフ確認待ち仮予約 |
| POST /api/requests/{public_id}/review | `status: confirmed/rejected`。confirmed時は`calendar_id`必須 |
| GET /api/bookings/{line_user_id} | 指定利用者の予約履歴 |
| GET /api/waitlist | キャンセル待ちの提案・回答一覧 |
| POST /api/check-waitlist | 空きチェックと提案作成 |
| POST /api/notifications/retry | 未送信通知を最大5件処理 |
| GET /api/notifications | 未送信・手動確認が必要な通知（最大100件） |

管理APIはスタッフ用の連携口です。一般利用者向けの認証なし管理画面はありません。

## Scheduler

既存のキャンセル待ちジョブは監査時点でPAUSEDでした。停止理由を確認するまで再開しません。再開時はGETをPOSTへ変更し、認証ヘッダーを追加してください。トークン付きScheduler設定をログやGitへ書き込まないでください。

通知Outboxの処理には別の`POST /api/notifications/retry`ジョブを1分間隔で設定します。Webhook処理の終了時にも処理しますが、ユーザー操作がない間の再送にはSchedulerが必要です。

## 通知失敗

受付と通知Outboxの保存は同じトランザクションです。LINEへの再送は同じ`X-Line-Retry-Key`を使用します。LINE側の重複防止期間を超える恐れのある23時間経過の未解決通知は`review`にし、自動送信しません。管理者は送達状況を確認し、必要な連絡を個別に行ってください。自動で成功と見なさないでください。

## キャンセル待ち

提案への30分の回答制限はありません。ボタンは提案した予約の開始時刻まで有効で、承諾時には最新の空き・通常の受付条件を再確認します。本人・現在状態・空きを検証し、承諾/辞退と管理者通知をDBへ保存します。承諾も仮予約の希望受付であり、枠の確保ではありません。失効した提案を再度出す場合はスタッフが待機内容を確認して新しい登録日時のエントリーを作成します。

既存シートはA〜G列を維持します。先頭6列の内容ハッシュを識別子にし、更新直前に行を探し直します。同一内容の行が複数ある場合や内容が変わった場合は更新を止めます。SheetsにはDB相当の条件付き更新がないため、**状態反映中の行の並べ替え・挿入は避けてください**。DBの回答状態を正として照合してください。シート編集と完全に競合しない構成は、待機登録もDBへ統一する次段階の改修が必要です。

## 顧客マスタ・本人識別

`CUSTOMER_SHEET_NAME`でタブ名を指定します。顧客情報は60秒キャッシュし、更新後の取得が失敗した場合に期限切れの情報で認可しません。空の必須項目・重複LINE IDはエラーになります。

カレンダーの`extendedProperties.private.line_user_id`がある場合はそれを優先します。旧予定はフルネームの境界一致のみ使用し、短い名前・同姓同名など曖昧な照合は行いません。該当利用者はIDの関連付けが必要です。部分一致へ戻さないでください。

## ヘルス・ログ

`/`はプロセス応答、`/health`は設定・DB・カレンダー取得状態・LINE初期化のreadinessです。必須設定不足や外部連携の起動確認失敗は起動を止めます。アプリエラーは構造化ログへ出し、メッセージ本文・氏名・LINE IDを通常ログへ出しません。

日次で未処理予約、通知バックログ、Scheduler成功、Cloud Runの5xx/応答時間を確認してください。カレンダーの取得失敗は空きゼロや空き多数と混同せず「確認できない」と表示します。

## バックアップ

本番はFirestoreの無料枠を利用します。有料のPITR・管理バックアップ・TTL課金は有効化しません。移行時のJSONバックアップはアクセス制限したローカル保管先へ保存します。Firestoreの永続性は誤削除の履歴復元を保証しないため、破壊的変更前に退避してください。移行元SQLiteは非公開の場所に保存し、Git・コンテナには含めません。


## トーク内の待ち希望・スタッフ相談

トークからの待ち希望はFirestoreへ受付と管理者通知を同時保存します。既存のキャンセル待ち確認ジョブが日時・店舗・時間帯の条件に合う空きを探し、LINEの回答カードをOutbox経由で通知します。既存のSheets待ち一覧も引き続き処理しますが、トーク受付分をSheetsへ投影しません。

待ち希望の取り下げは通常予約の取消と独立しています。取り下げ後の未送信の空き提案は送信しません。既に送信中・送信済みのメッセージは撤回できませんが、回答時にも現在状態を検証します。希望日を過ぎた待機エントリーは終了扱いになります。空き提案には30分の回答制限を設けず、自動再提案はしません。再度希望する場合は新しく申し込んでください。

スタッフ相談は内容確認後に管理者の通知Outboxへ保存します。回答はスタッフが行います。キャンセル待ち・相談を終えると、中断前の予約セッションを復元します。

検索は通常のCalendar取得結果を使い、期間内の候補をページ表示します。最短検索は受付可能期間内、満席時の近隣日提案は翌日から7日以内です。追加のLLM利用料・新しい定期ジョブは発生させません。既存ジョブと通知に伴う通常のサービス利用量は増えるため、無料枠の利用量は引き続き監視してください。


## 管理者LINEからの承認

既存の `ADMIN_USER_ID` に届く予約通知を、名前・日時・店舗・個室希望と「✅ 確定する」「今回は見送る」のカードにします。新しい公式アカウントや別チャネルの設定は追加しません。

1. スタッフが通常どおりGoogleカレンダーへ予約を登録します。
2. 管理者が通知カードの「確定する」を押します。最新カレンダーから本人・開始日時・店舗が一致する予定を1件に特定して承認します。予定IDの手入力は不要です。一致なし・複数一致・取得失敗の場合は確定せず、登録の確認を案内します。
3. 確定状態とお客様宛ての結果カードを同一トランザクションで保存し、既存の通知Outboxから送信します。再送では同じretry keyを使用します。

「今回は見送る」は別の確認カードを表示し、「見送ってお客様に通知」で却下と通知を実行します。「戻る・まだ判断しない」は状態を変えません。カレンダーの登録・編集・削除はどのボタンでも実行しません。

管理者が「承認待ち」「予約受付一覧」「管理メニュー」と送ると未処理の予約を呼び出せます。以前に届いたテキスト通知の予約も対象です。カードは9件ずつ表示し、続きはボタンで表示できます。通知の承認ボタンは7日間、見送りの最終確認は30分間有効です。期限切れの場合は「承認待ち」で更新してください。開始時刻を過ぎた予約はLINEの確定ボタンでは承認しません。

管理者本人以外の操作・処理済み・取消済み予約の再承認を拒否します。カレンダーに登録済みでも、対応する仮予約が未承認なら予約一覧は仮予約表示を維持します。仮予約要求がない従来のカレンダー予定は引き続き確定予約として扱います。


## キャンセル待ちの送信通数

待ち希望の受付で管理者へ1通、空き提案で本人へ1通、承諾・辞退で管理者へ1通のPushを送ります。したがって一巡で基本3通です。予約確定、取り下げ、再申し込み、通常の予約通知、一斉配信は別途加算します。15分ごとの確認自体はLINEを送らず、同じエントリーを繰り返し提案しません。30分回答制限の撤廃による定期ジョブ・再通知の追加はありません。

LINEの応答メッセージは通数対象外、Pushは対象です。無料プランの月200通を超えると送信エラーになり、自動で有料プランへ切り替わるわけではありません。最新のプラン・通数は管理画面またはMessaging APIのquota/consumptionで確認してください。[公式料金](https://developers.line.biz/ja/docs/messaging-api/pricing/)

15分間隔は1日96回、30日で2,880回の確認です。既存ジョブを使うため新しい固定費はありませんが、待機件数に応じてFirestoreの読み取り・Cloud Runの処理時間などは増えます。無料枠は他の処理と合算されるため、総費用0円を保証しません。
